/** Wheel filename from `uv build` in the Python package root (parent of `github-io/`); keep version in sync. */
export const LOCAL_STARTER_WHEEL_FILE = "starter_template-0.1.1-py3-none-any.whl";

/** URL path segment (under Vite `base`, e.g. `/repo/local-starter/...` on GitHub Pages). */
export const LOCAL_STARTER_URL_SEGMENT = "local-starter";
